from random import randrange

def generate_random_playlist(diccionario):
    assert isinstance(diccionario, dict)w<
    lista_canciones = []




    assert len(lista_canciones) == len(diccionario)
    return lista_canciones[]